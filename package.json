{
  "name": "wallpaper-roll-planner",
  "version": "1.0.0",
  "private": true,
  "description": "A private, browser-only wallpaper roll calculator with pattern-repeat cut planning.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
